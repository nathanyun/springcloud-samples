# 服务端口
server.port=8002
# eureka的主机名称
eureka.instance.hostname=eureka2
# 客户端配置-注册中心访问地址, eureka1是在Hosts配置的映射，替代域名的作用。
# 因为是高可用配置，所以这里配置的是对方的地址
eureka.client.service-url.defaultZone=http://eureka1:8001/eureka,http://eureka3:8003/eureka
# 是否启用自我保护
eureka.server.enable-self-preservation=true
# 定时剔除不可用的服务列表，默认为60s
eureka.server.eviction-interval-timer-in-ms=30000
# 客户端向服务器端发送心跳间隔，默认为30s
eureka.instance.lease-renewal-interval-in-seconds=30
# 客户端租期到期后30内，若Eureka服务器还没收到该客户端心跳的话，则会标识客户端可以被剔除了，默认为90s
eureka.instance.lease-expiration-duration-in-seconds=33